# {{BRAND_NAME}} Instance

`{{INSTANCE_REPO_NAME}}` 是 `{{TEMPLATE_REPO_NAME}}` 模板工程的实例下游仓库，用来承接当前真实实例的非敏感映射、验收结论、部署入口，以及后续必要的实例差异。

它不是：

- `{{TEMPLATE_REPO_NAME}}` 的第二条模板源码主线
- `{{DEPLOY_REPO_NAME}}` 的平行部署引擎
- 保存 secrets、密码、token、私钥的仓库

## 四层关系

当前主线固定为四层：

1. `{{TEMPLATE_REPO_NAME}}`
   - 模板源码仓库
   - 负责源码、契约、镜像构建与发布前 gate
2. `{{DEPLOY_REPO_NAME}}`
   - 独立部署引擎仓库
   - 负责 `testing / production` 发布、回滚、smoke 与 workflow
3. `{{INSTANCE_REPO_NAME}}`
   - 当前仓库
   - 负责实例级非敏感映射、验收记录、实例同步入口与必要的实例差异
4. 真实服务器
   - 负责容器运行、反向代理、数据库、Redis、证书与 runner

## 当前固定模型

- 远程环境只保留：
  - `testing`
  - `production`
- 触发规则固定为：
  - `{{TEMPLATE_REPO_NAME}} main` push -> `testing` 自动发布
  - `production` 只在 `{{DEPLOY_REPO_NAME}}` 手动提升
- `local` 仍属于 `{{TEMPLATE_REPO_NAME}}` 模板仓库，不进入 `{{DEPLOY_REPO_NAME}}` 环境矩阵

## 固定入口

- [CLAUDE.md](./CLAUDE.md)
- [`.rtnn/instance.json`](./.rtnn/instance.json)
- [`.rtnn/acceptance.md`](./.rtnn/acceptance.md)
- [`scripts/check-instance-boundary.mjs`](./scripts/check-instance-boundary.mjs)
- [`scripts/sync-from-template.sh`](./scripts/sync-from-template.sh)
- [`scripts/render-runtime-env.mjs`](./scripts/render-runtime-env.mjs)

## 当前实例入口

testing 正式验收入口固定为：

- `https://{{TESTING_ROOT}}`
- `https://{{TESTING_API}}`
- `https://{{TESTING_ADMIN}}`
- `https://{{TESTING_APP}}`
- `https://{{TESTING_WEAPP}}`

production 正式入口固定为：

- `https://{{PRODUCTION_ROOT}}`
- `https://{{PRODUCTION_API}}`
- `https://{{PRODUCTION_ADMIN}}`
- `https://{{PRODUCTION_APP}}`
- `https://{{PRODUCTION_WEAPP}}`

## 同步主线

当前实例仓库的同步规则固定为：

1. 通用能力永远先改 `{{TEMPLATE_REPO_NAME}}`
2. `{{INSTANCE_REPO_NAME}}` 只保留实例差异与实例契约
3. `{{INSTANCE_REPO_NAME}}` 不 merge `{{TEMPLATE_REPO_NAME}}` 源码，只刷新模板托管的薄实例资产
4. 模板同步与模板引用校验都通过临时模板工作区完成
5. 禁止手工挑“关键文件”复制到实例仓库

首次配置 upstream remote：

```bash
./scripts/sync-from-template.sh --remote-only
```

后续刷新模板托管资产：

```bash
./scripts/sync-from-template.sh
```

若要验证某个模板分支、tag 或 commit，但暂不落地当前实例文件：

```bash
./scripts/sync-from-template.sh --check --ref main
```

## 同步与防污染机制

当前固定机制是：

1. `{{INSTANCE_REPO_NAME}}` 只保留薄实例资产，不承接 `apps / packages / docs / compose` 等模板源码目录
2. 允许跟踪的文件白名单，以 [`.rtnn/instance.json`](./.rtnn/instance.json) 里的 `boundary.allowedTrackedPaths` 为准
3. 未忽略的新文件也受同一份白名单约束，避免实例仓库先被污染再提交
4. 同步脚本会先做边界检查，再在临时模板工作区里生成一份新的实例资产
5. 当前仓库只会刷新 `sync.templateManagedPaths` 里的模板托管文件，不会 merge 模板源码目录
6. 当前仓库会保留 `sync.instanceOwnedPaths` 里的实例文件，并记录本次刷新的模板 ref / commit

手工检查当前实例是否被污染：

```bash
node ./scripts/check-instance-boundary.mjs
```

如果未来确实需要新增实例文件，先改 `boundary.allowedTrackedPaths`，再新增文件；不要先加文件再解释原因。

## 实例驱动的 deploy env 渲染

当前 runtime env 统一从实例契约渲染：

```bash
node ./scripts/render-runtime-env.mjs --environment testing
node ./scripts/render-runtime-env.mjs --environment production --version v1.0.0
```

脚本会优先回退到模板仓库 `.env` 中的默认值，足以支撑本地或私有验收。

若要生成真实可发布环境，仍应显式通过环境变量或参数覆盖敏感值：

- `<PROJECT>_TESTING_DB_PASSWORD`
- `<PROJECT>_TESTING_JWT_ACCESS_SECRET`
- `<PROJECT>_TESTING_JWT_REFRESH_SECRET`
- `<PROJECT>_PRODUCTION_DB_PASSWORD`
- `<PROJECT>_PRODUCTION_JWT_ACCESS_SECRET`
- `<PROJECT>_PRODUCTION_JWT_REFRESH_SECRET`

当前项目前缀为：`{{PROJECT_ENV_PREFIX}}`
